language: dart

dart:
  - 2.8.4
  - dev

dart_task:
  - test: --platform vm
    xvfb: false

# Only building master means that we don't run two builds for each pull request.
branches:
  only: [master]

cache:
  directories:
    - $HOME/.pub-cache